# M. Gunaseelan — Portfolio

A responsive vanilla HTML/CSS/JavaScript portfolio with dark/light themes, accessible navigation, subtle scroll reveals, and client-side form validation.

## Run locally

Open `index.html` in a browser, or serve this directory through a static server.

## Before publishing

- The contact email is set to the existing address `gunaseelan25122006@gmail.com`; update it if needed.
- Confirm or replace the existing GitHub and LinkedIn links.
- Keep `resume.pdf` up to date.
- Add real project links and update project statuses as projects are completed.

The profile image is `assets/profile-photo.jpg`; certificates are in `certificates/`.
